@if (GetFieldItemActive())
{
    <ilw-content>
        <div class="ils-input-entry">
            <label for="@Id">@Title</label>
            <textarea id="@Id" @bind="Value" @bind:after="Layout.SetDirty" />
            <p>@GetFieldItemDescription()</p>
        </div>
    </ilw-content>
}